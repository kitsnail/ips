---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ips-data
  namespace: ips
  labels:
    app: ips
    component: apiserver
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-csi-0
